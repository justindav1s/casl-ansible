- name: Create App VM scaleset
  azure_rm_virtualmachine_scaleset:
    resource_group: "{{ ocp_rg_name }}"
    name: "{{ app_vm_name }}-scaleset"
    vm_size: "{{ app_vm_size }}"
    capacity: "{{ num_apps }}"
    os_type: Linux
    ssh_password_enabled: false
    storage_account: "{{ storage_account_name }}"
    managed_disk_type: "{{ storage_account_type }}"
    admin_username: "{{ admin_user }}"
    subnet_name: "{{ app_subnet_name }}"
    ssh_public_keys:
      - path: "/home/{{ admin_user }}/.ssh/authorized_keys"
        key_data: "{{ ssh_publickey }}"
    network_interface_names:
    - "{{ app_networkinterface_name }}"
    data_disks:
    - lun: 0
      disk_size_gb: "{{ app_docker_vol_size }}"
      storage_container_name: "{{ app_vm_name }}-docker-vol"
      managed_disk_type: "{{ storage_account_type }}"
      storage_account_name: "{{ storage_account_name }}"
    image:
      offer: "{{ image_offer }}"
      publisher: "{{ image_publisher }}"
      sku: "{{ image_sku }}"
      version: "{{ image_version }}"
    tags:
      app: "true"
      type: "app"
      name: "{{ app_vm_name }}"
      group: "{{ group_apps_tag }}"
      node_labels: "{{ labels_apps_tag }}"
      env_id: "{{ env_id }}"